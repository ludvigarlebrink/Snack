-- Called when the object is created.
function SomeScript:OnBegin(transform)
	-- Code is placed here.
end

-- Called when the object is destroyed.
function SomeScript:OnEnd(transform)
	-- Code is placed here.
end

-- Called once every frame.
function SomeScript:OnTick(transform, deltaTime)
	-- Code is placed here.
end
